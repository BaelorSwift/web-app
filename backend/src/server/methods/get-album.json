{
	"type": "object",
	"additionalProperties": false,

	"required": [
		"albumSlug"
	],

	"properties": {
		"albumSlug": {
			"type": "string",
			"pattern": "[a-zA-Z0-9-]+",
			"minLength": 1
		}
	}
}
